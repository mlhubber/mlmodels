# We need to set up proc/fashionTexture to contain the pre-trained ML
# models:
#
#  cntk.model;
#  svm.np
#
# This setup script should take care of it. We do not put them into
# github (too large). See the resnet152 model for an exapmle.
